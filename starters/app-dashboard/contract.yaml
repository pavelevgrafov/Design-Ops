meta: {schema_version: "6.0", mode: quick, artifact_profile: app}
product: {name: "Starter: app-dashboard", category: operations tool, risk_level: medium}
experience:
  primary_job: "When the shift starts, I want to see today's orders and their statuses, so I can react to late ones first."
  key_screens: [{id: orders, name: "Orders", purpose: "table + KPI + create form"}]
  required_states: [loading, empty, error, success, permission_denied]
content_model: {pattern: task-first, pattern_reason: "the user came to work the queue, not to read"}
starters: {route: starter_first, chosen: app-dashboard}
gates: {gate1: passed, gate2: deferred, gate2_deferred: true, mode: batched}
status: {base_skin_applied: true, verdict: ready_with_caveats}
visual: {}
acceptance: {}
